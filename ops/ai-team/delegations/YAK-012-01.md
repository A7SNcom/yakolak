# YAKOLAK-DELEGATION:yak012-playable-handoff-cd82161

المهمة: YAK-012-01 — استعادة انتقال قابل للعب بعد رحلة البداية.

## كمال

لا يوجد تكليف الآن.

## كامل

نفّذ الإصلاح على فرع `agent/kamel/YAK-012-01` وDraft PR إلى `agent/yakolak-team-os`.

النتيجة: بعد انتهاء رحلة البداية تظهر واجهة الإعداد الأصلية أو يصبح مسار اللعب الأصلي متاحًا فورًا، وتعود قابلية النقر للـcanvas. عند فشل التجهيز تظهر رسالة عربية مرئية وزر إعادة محاولة.

المجال المسموح: `index.html` و`app.js` و`src/entry-v126.js` و`scripts/verify-v126-clean-entry.mjs` فقط.

الممنوع: منطق اللعبة، القواعد، الشبكات، Production، main، الأسرار، الصلاحيات، أو إنشاء واجهة إعداد موازية.

الدليل المطلوب: فرق الملفات، `pnpm test`، لقطة أو تسجيل لمسار البداية ثم الإعداد على الكمبيوتر والجوال، وعدم وجود تجاوز أفقي. مع `prefers-reduced-motion` يجب تجاوز الحركة الكاملة.

شرط التوقف: أي حاجة لتعديل ملف خارج المجال أو تغيير عقد اللعبة.

## مكمل

راجع Draft PR كامل مستقلًا بعد توفره. تحقق أن الإصلاح يعيد استخدام واجهة الإعداد الأصلية ولا يضيف مصدر حالة أو مسار دخول موازٍ، وأنه لا يمس القواعد أو الشبكات. أعط PASS أو HOLD بدليل فرق الملفات والفحوص.

## كميليا

بعد نتيجة كامل ومراجعة مكمل، تحققي على رأس Draft PR نفسه من الكمبيوتر والجوال: اكتمال رحلة البداية، ظهور واجهة الإعداد الأصلية، قابلية النقر، رسالة الفشل المرئية وإعادة المحاولة، ومسار reduced-motion. لا تصلحي العمل أثناء التحقق.
