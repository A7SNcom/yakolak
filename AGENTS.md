# AGENTS.md — Yakolak AI Developer Guide

هذا الملف مخصص لأي AI Agent أو مطور أو جلسة ChatGPT جديدة تكمل تطوير مشروع Yakolak بدون تخبط.
اقرأ هذا الملف قبل أي تعديل.

## 1. طريقة التواصل مع صاحب المشروع

صاحب المشروع: أحمد خليفة / أبو كمال.

أسلوب العمل المطلوب:

- المستخدم ليس مبرمجاً، لذلك لا ترسل تعليمات تقنية طويلة.
- المستخدم يختبر بصرياً من الجوال غالباً.
- المطلوب إجابات قصيرة وواضحة.
- عند الخطأ اعترف مباشرة ولا تبرر.
- لا تنفذ قفزات كبيرة من نفسك.
- لا تضيف عناصر أو مكتبات أو منطق جديد إلا إذا طلب صراحة.
- أي تعديل يجب أن يكون صغيراً ومحدداً وقابل للرجوع.
- بعد كل تعديل اذكر فقط ما تم و Commit ID.

## 2. المستودع والرابط

```txt
Repository: A7SNcom/yakolak
Branch: main
GitHub Pages: https://a7sncom.github.io/yakolak/
Main file: index.html
Constants file: PROJECT_CONSTANTS.md
Agent guide: AGENTS.md
```

## 3. قاعدة ذهبية

لا تلمس ثوابت `9` و `3` إلا بطلب صريح جداً من أحمد.

أي تطوير جديد يجب أن يبنى فوق النسخة المستقرة ولا يكسرها.

## 4. الثوابت المعتمدة للمشروع

النسخة المستقرة الأساسية تحتوي:

```txt
9.stl
3.stl
```

`9.stl` هو البورد الأساسي.

`3.stl` هو قطعة واحدة يتم تكرارها أربع مرات حول البورد:

```js
const APPROVED_MODEL_TRANSFORMS = {
  "9": {
    "px": 0,
    "py": 6,
    "pz": 0,
    "rx": -90,
    "ry": 0,
    "rz": 0
  },
  "3-right": {
    "px": 135,
    "py": 6,
    "pz": 0,
    "rx": -90,
    "ry": 0,
    "rz": 0
  },
  "3-left": {
    "px": -135,
    "py": 6,
    "pz": 0,
    "rx": -90,
    "ry": 0,
    "rz": 180
  },
  "3-front": {
    "px": 0,
    "py": 6,
    "pz": 135,
    "rx": -90,
    "ry": 0,
    "rz": 90
  },
  "3-back": {
    "px": 0,
    "py": 6,
    "pz": -135,
    "rx": -90,
    "ry": 0,
    "rz": -90
  }
};
```

## 5. شرح المحاور في هذا المشروع

مهم جداً:

- في برامج التصميم غالباً Z يعني ارتفاع.
- في Three.js داخل هذا المشروع الارتفاع المرئي هو غالباً Y.
- أحمد قد يقول: "الزي" ويقصد بها الارتفاع وليس محور Z البرمجي.
- لا تكتب للمستخدم "Z" إذا تقصد ارتفاع. اكتب "الارتفاع".

القاعدة العملية:

```txt
ارتفاع مرئي = py
Z / pz = حركة على السطح / العمق، وليس الارتفاع في هذا المشهد
```

## 6. LMS

الملفات:

```txt
l.stl
m.stl
s.stl
```

يتم وضعها كمجموعة واحدة اسمها في الواجهة:

```txt
LMS
```

قواعد LMS:

1. هي ثلاث مجسمات فقط: `l/m/s`.
2. ليست أطقم متعددة.
3. توضع في مركز البورد مبدئياً.
4. أدنى نقطة للثلاثة يجب أن تكون متساوية داخلياً.
5. عند اختيار `LMS` في المعايرة، يظهر إعداد واحد فقط: `الارتفاع`.
6. لا تستخدم كلمة Z في واجهة LMS.
7. عند معايرة LMS تظهر طبقة قاعدة شفافة لتوضيح مكان قاعدة المجموعة.
8. القاعدة الشفافة تظهر فقط أثناء معايرة LMS وتختفي عند إخفاء المعايرة أو اختيار موديل آخر.

## 7. واجهة المعايرة الحالية

واجهة المعايرة صممت للجوال.

السلوك المطلوب:

- زر الإعدادات ⚙ يفتح Wizard بسيط.
- كل مرة يظهر سؤال واحد فقط.
- السؤال الأول: إيش تبغى تعاير؟
- إذا اختار المستخدم LMS، تظهر أداة معايرة صغيرة مباشرة لأن LMS فيه إعداد واحد فقط.
- إذا اختار 9 أو أحد 3، يظهر سؤال ثاني لاختيار الخاصية.
- أداة المعايرة الصغيرة يجب ألا تحجب المشهد.
- يجب أن تظل المعاينة واضحة أثناء السحب والتعديل.
- تحتوي أداة المعايرة على مدخل رقم واحد فقط للمحور/الخاصية الحالية.
- أزرار + و - تتحرك خطوة واحدة فقط، وليس 10.

## 8. قواعد تصميم الجوال

- لا تجعل لوحة الإعدادات تغطي كامل المشهد.
- استخدم شريط معايرة صغير عائم في الأسفل.
- الأزرار يجب أن تكون كبيرة ومريحة للمس.
- لا تجعل المستخدم يفتح ويغلق الإعدادات كل مرة ليرى النتيجة.
- المعاينة أهم من اللوحة.
- تجنب النصوص الطويلة داخل الواجهة.

## 9. طريقة التطوير الصحيحة

قبل أي تعديل:

1. اقرأ `AGENTS.md`.
2. اقرأ `PROJECT_CONSTANTS.md`.
3. اجلب آخر نسخة من `index.html`.
4. حدد بالضبط ما المطلوب تغييره.
5. لا تغيّر أي شيء خارج المطلوب.

بعد أي تعديل:

1. اذكر ما تغير باختصار.
2. اذكر Commit ID.
3. لا تزعم أنك اختبرت بصرياً إلا إذا فعلاً تم إرفاق صورة أو نتيجة من المستخدم.

## 10. ممنوعات مهمة

ممنوع:

- تغيير مواقع `9` أو `3` بدون طلب صريح.
- تحويل LMS إلى أطقم متعددة بدون طلب صريح.
- إضافة مكتبات ضخمة أو Framework كامل بدون سبب.
- تسمية الارتفاع Z في الواجهة.
- تنفيذ أكثر من طلب في نفس التعديل.
- إزالة إمكانية النسخ من المعايرة.
- كسر GitHub Pages أو importmap.
- استخدام Google Drive في هذا المشروع حالياً.

## 11. مكتبات المشروع الحالية

المشروع يستخدم حالياً:

```html
three: https://cdn.jsdelivr.net/npm/three@0.165.0/build/three.module.js
three/addons/: https://cdn.jsdelivr.net/npm/three@0.165.0/examples/jsm/
```

والملفات المستعملة:

```txt
index.html
9.stl
3.stl
l.stl
m.stl
s.stl
PROJECT_CONSTANTS.md
AGENTS.md
```

## 12. نظام النسخ والكاش

داخل `index.html` يوجد:

- meta no-cache.
- متغير `APP_VERSION`.
- assetVersion مع `Date.now()`.
- فحص تلقائي كل 30 ثانية لإعادة التحميل إذا تغيرت النسخة.

عند كل تعديل مهم غيّر `APP_VERSION` و meta version.

## 13. آخر حالة معروفة قبل إنشاء هذا الملف

آخر تعديل معروف قبل هذا الملف:

```txt
Commit: 2715375
Purpose: Make plus/minus calibration buttons step by one.
```

آخر سلوك معروف:

- الواجهة مناسبة للجوال.
- زر الإعدادات يفتح أسئلة خطوة بخطوة.
- LMS يظهر كخيار معايرة.
- LMS فيه إعداد واحد: الارتفاع.
- تظهر قاعدة شفافة أثناء معايرة LMS.
- أزرار + و - تتحرك خطوة واحدة.

## 14. أسلوب الرد المطلوب بعد التنفيذ

مثال جيد:

```txt
تم ✅

عدلت زر + و - ليحرك خطوة واحدة فقط.

Commit: abc1234
```

مثال سيئ:

```txt
قمت بإعادة هندسة النظام وإضافة مجموعة مزايا وتحسينات...
```

لا تعمل بهذه الطريقة.

## 15. عند الشك

إذا لم تفهم طلب أحمد:

- لا تنفذ.
- اسأله سؤال واحد فقط.
- لا تعطيه خمسة احتمالات.
- لا تفترض.

مثال:

```txt
تقصد أضيف التحكم على LMS فقط ولا على كل الموديلات؟
```

## 16. قاعدة الرجوع الآمن

لو أي تعديل خرب المشهد:

ارجع فوراً إلى النسخة المستقرة:

```txt
9 + أربع قطع 3
```

وتأكد من عدم العبث بثوابت `PROJECT_CONSTANTS.md`.
