# Yakolak STL Models

Live model files used by `app-hejaz-v043.js`.

```txt
9.stl   board
3.stl   outer bases
p.stl   p markers
l.stl   large stone
m.stl   medium stone
s.stl   small stone
```

These files are loaded through:

```js
const MODEL_DIR = './assets/models/';
```
