# Color Mode v037 — Historical Reference

## الحالة

```txt
Historical mode: v037-color-controls-only
Current live mode: index.html -> app.js -> app-hejaz-v043.js
```

هذه الصفحة توثيق تاريخي فقط. ليست مصدر الحقيقة للتشغيل الحالي.

## ماذا كانت هذه النسخة؟

كانت نسخة تعرض ألوان فقط ولا تعرض معايرات مواقع.

## عناصر الألوان التاريخية

```txt
base     = لون البورد 9.stl وكل قواعد 3.stl
p        = لون كل قطع p.stl
right    = لون حجار قاعدة اليمين
left     = لون حجار قاعدة اليسار
front    = لون حجار قاعدة الأمام
back     = لون حجار قاعدة الخلف
```

## حجار البورد في تلك النسخة

كانت حجار البورد مخفية بصريًا، لكن مواقعها محفوظة في المخرجات:

```txt
board_stones.positions
```

## ممنوع الخلط

لا تستخدم هذا الملف لتحديد الحالة الحالية للموقع. الحالة الحالية موثقة في:

```txt
docs/LIVE_STATE.md
docs/CURRENT_GOLDEN_STATE.md
docs/AI_AGENT_HANDOFF.md
```

## سبب إبقاء الملف

هذا الملف محفوظ كمرجع تاريخي حتى لا تضيع القرارات السابقة، وليس لأنه مسار التشغيل الحالي.
