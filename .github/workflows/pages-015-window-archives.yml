name: PAGES-015 Frontend Rollback Archives (retired)
# Retired manual placeholder. The authoritative archive path is the main
# PAGES-015 qualification orchestrator, which checks out threejs-rebuild and
# uses the Admin-gated v2 archive helper under the shared ledger lock.

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages-release-qualification-ledger
  cancel-in-progress: false

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse legacy archive mutation path
        shell: bash
        run: |
          echo '::error title=PAGES-015 legacy archive fallback retired::Use the PAGES-015 qualification orchestrator on main. This workflow intentionally cannot publish releases or write qualification evidence.'
          exit 64
