name: PAGES-012 Preserve Exact Source Bytes

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Historical PAGES-012 source preservation is retired
        shell: bash
        run: |
          set -euo pipefail
          echo 'PAGES-012 source preservation is historical evidence only.'
          echo 'This workflow is intentionally read-only and manual-only.'
          echo 'PAGES-015 resolves exact active+previous source bytes through its locked recovery chain.'
          echo 'Use RELEASE_QUALIFICATION/PAGES015_RECOVERED_SOURCES.json and deterministic reconstruction helpers; do not recreate the old PAGES-012 source artifact.'
